import { NgxWatermarkDirective } from './ngx-watermark.directive';

describe('NgxWatermarkDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxWatermarkDirective();
    expect(directive).toBeTruthy();
  });
});
